# base/kustomization.yaml
resources:
  - namespace.yaml
  - runner.yaml
  - resource-quota.yaml
  - network-policy.yaml
  - docker-socket-hardening.yaml

labels:
  managed-by: kustomize
  component: ci-runner